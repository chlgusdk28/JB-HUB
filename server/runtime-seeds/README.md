Sample runtime files live here so the repository root stays focused on source code.

- `project-files/`: example project attachments copied into `.runtime/storage/project-files/` on first run

These files are seeds, not live runtime state. Actual uploads and generated files stay under `.runtime/`.
