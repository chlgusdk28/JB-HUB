# JB-Hub 전수조사 개선항목 리스트

> 작성일: 2026-03-01
> 분석 대상: `c:\Users\Hyeona\Desktop\JB-Hub`
> 총 발견 항목: 43개 (12개 카테고리)

---

## 🚨 P0 — 즉시 조치 필요 (보안 치명적)

| # | 항목 | 파일 | 설명 |
|---|------|------|------|
| 1 | `.env` 파일에 실제 비밀번호/시크릿 하드코딩 | `.env` | DB 비밀번호, JWT 시크릿 등 민감정보가 파일에 평문 저장됨 |
| 2 | JWT 시크릿이 플레이스홀더 값 그대로 사용 | `.env.example:22` | `replace-with-long-random-secret` 값 사용 시 토큰 위조 가능 |
| 3 | MySQL root 비밀번호가 `root` 기본값 사용 | `docker-compose.yml` | 기본 자격증명 노출로 DB 무단 접근 위험 |
| 4 | `.gitignore`에 `.env` 포함 여부 확인 필요 | `.gitignore` | 리포지토리에 커밋될 경우 자격증명 완전 노출 |

---

## 🔴 P1 — 높은 우선순위

### 보안

| # | 항목 | 파일 | 설명 |
|---|------|------|------|
| 5 | JWT 시크릿 없어도 서버 기동 가능 | `server/index.js:1802` | 경고만 출력하고 종료하지 않음. JWT 모드에서 시크릿 없으면 기동 실패해야 함 |
| 6 | `x-user-id` 헤더로 신원 스푸핑 가능 | `server/index.js:36` | `API_ALLOW_DEV_HEADER_AUTH` 옵션이 프로덕션에서 실수로 활성화될 수 있음 |
| 7 | 에러 응답에 서버 내부 정보 노출 위험 | `server/index.js:553` | 에러 메시지에 구현 세부사항 포함 가능. 외부 노출 전 sanitize 필요 |

### 코드 품질

| # | 항목 | 파일 | 설명 |
|---|------|------|------|
| 8 | 폐기된 `document.execCommand('copy')` 사용 | `src/app/hooks/useAppActions.ts:296` | 브라우저 비표준 API. `navigator.clipboard.writeText()`로 교체 필요 |
| 9 | `as any` 타입 단언 남용 | `src/app/components/OpalProjectDetail.tsx:90`, `ProjectDetailPage.tsx:70` | TypeScript 타입 검사 우회. 적절한 타입 정의 필요 |
| 10 | TypeScript strict 모드 미적용 | `tsconfig.json` | `"strict": true` 미설정으로 잠재적 타입 오류 미검출 |

---

## 🟠 P2 — 중간 우선순위

### 성능

| # | 항목 | 파일 | 설명 |
|---|------|------|------|
| 11 | `audit_logs.created_at` 인덱스 없음 | `server/index.js:637` | ORDER BY 쿼리 시 풀 테이블 스캔 발생. `INDEX idx_created_at (created_at)` 추가 필요 |
| 12 | 인메모리 모드 검색 O(n) 전체 스캔 | `server/index.js:1313` | 프로젝트 수 증가 시 검색 성능 선형 저하 |
| 13 | 프론트엔드 데이터 캐싱 없음 | `src/app/App.tsx:148` | 컴포넌트 재마운트 시마다 API 재호출. SWR 또는 React Query 도입 권장 |
| 14 | 인메모리 rate limiter 무한 증가 가능 | `server/index.js:469` | IP별 버킷 정리 타이밍 문제로 메모리 누수 가능. 주기적 정리 인터벌 추가 필요 |

### 아키텍처 / 설계

| # | 항목 | 파일 | 설명 |
|---|------|------|------|
| 15 | 복잡한 DB 작업에 트랜잭션 없음 | `server/index.js` | 유저 생성 + 감사로그 기록이 별도 쿼리 — 중간 실패 시 데이터 불일치 가능 |
| 16 | 프론트엔드 페이지네이션 없음 | `src/app/App.tsx` | 전체 프로젝트를 한 번에 불러와 클라이언트 사이드 필터링. 대규모 데이터 시 성능 저하 |
| 17 | MySQL 모드 vs 인메모리 모드 동작 불일치 | `server/index.js:1241` | 두 구현체 간 기능 차이 존재. 테스트 신뢰도 저하 |
| 18 | URL 해시 + 내부 state 이중 관리 | `src/app/App.tsx:381` | 두 상태 간 동기화 오류 발생 가능 |
| 19 | 다른 사람 대신 프로젝트 생성 불가 | `server/index.js:324` | 작성자가 인증된 사용자로 강제 고정되어 관리자 위임 불가 |

### 데이터 무결성

| # | 항목 | 파일 | 설명 |
|---|------|------|------|
| 20 | 감사 로그 보존 정책 없음 | `server/index.js` | 로그가 무한 증가. 자동 삭제/아카이브 정책 필요 |
| 21 | `DB_SEED=true` 프로덕션 실수 적용 위험 | `.env.example:9` | 환경 혼동 시 프로덕션 데이터 덮어쓰기 가능 |
| 22 | Soft delete 미구현 | `server/index.js` | 유저 삭제 시 감사로그에 고아 레코드 발생. `is_deleted` 컬럼 추가 권장 |

### 설정 / 환경

| # | 항목 | 파일 | 설명 |
|---|------|------|------|
| 23 | 서버 시작 시 필수 환경변수 검증 없음 | `server/index.js:1~61` | 잘못된 설정으로 기동해도 런타임에서야 오류 발생 |
| 24 | `CORS_ORIGINS` 미설정 시 프로덕션 요청 전부 차단 | `.env` | 프로덕션 배포 시 CORS 미설정으로 API 완전 차단 위험 |

### 테스트

| # | 항목 | 파일 | 설명 |
|---|------|------|------|
| 25 | 프론트엔드 컴포넌트 단위 테스트 전무 | `src/app/` | 컴포넌트 동작 변경 시 회귀 감지 불가 |
| 26 | 유틸리티 함수 단위 테스트 없음 | `src/app/lib/` | `project-utils.ts`, `localization.ts` 등 핵심 로직 미검증 |

### 국제화 (i18n)

| # | 항목 | 파일 | 설명 |
|---|------|------|------|
| 27 | UI 텍스트 전부 한국어 하드코딩 | 전 컴포넌트 | i18n 라이브러리 미사용. 다국어 지원 시 전체 코드 수정 필요 |
| 28 | 상대 날짜 표시도 한국어만 지원 | `src/app/lib/localization.ts` | `date-fns` 등 i18n 지원 날짜 라이브러리 도입 권장 |

---

## 🟡 P3 — 낮은 우선순위

### 코드 품질

| # | 항목 | 파일 | 설명 |
|---|------|------|------|
| 29 | `dangerouslySetInnerHTML` CSS 주입 패턴 | `src/app/components/ui/chart.tsx:83` | CSS는 안전하지만 패턴 자체가 위험. CSS 모듈 또는 `<style>` 태그로 대체 권장 |
| 30 | JSON 파싱 결과 타입 검증 없음 | `src/app/hooks/usePersistentState.ts:35` | `as unknown` 단언 후 실제 타입 확인 없음. Zod 등으로 런타임 검증 추가 권장 |
| 31 | 콜백 props 매 렌더마다 새 참조 생성 가능 | 다수 컴포넌트 | `onProjectClick` 등 콜백을 `useCallback`으로 메모이제이션 필요 |

### 의존성

| # | 항목 | 파일 | 설명 |
|---|------|------|------|
| 32 | MUI + Radix UI + Tailwind CSS 혼용 | `package.json` | 세 개의 UI 시스템 혼용으로 번들 크기 불필요하게 증가 |
| 33 | 버전 범위 지정(`^`, `~`) 사용 | `package.json` | 자동 마이너/패치 업데이트로 예상치 못한 동작 변경 가능 |

### DevOps / 배포

| # | 항목 | 파일 | 설명 |
|---|------|------|------|
| 34 | 프로덕션 중앙 로깅 미구성 | `server/index.js` | stdout만 사용. 프로덕션에서는 중앙 로깅 시스템(ELK, Datadog 등) 연동 필요 |
| 35 | 소스맵 비활성화로 프로덕션 디버깅 어려움 | `vite.config.ts:31` | `sourcemap: false` 설정으로 에러 스택트레이스 추적 불가 |
| 36 | `/api/health` 응답에 API 버전 정보 없음 | `server/index.js:1819` | 로드밸런서/모니터링 도구에서 버전 식별 불가 |
| 37 | Graceful shutdown 타임아웃 미설정 | `server/index.js:1810` | 장시간 처리 중인 요청이 강제 종료될 수 있음 |
| 38 | 로드밸런서용 헬스체크 문서화 없음 | - | 헬스체크 엔드포인트 존재하나 인프라 설정 문서 없음 |

### 문서화

| # | 항목 | 파일 | 설명 |
|---|------|------|------|
| 39 | REST API OpenAPI/Swagger 문서 없음 | `server/index.js` | API 엔드포인트 스펙이 코드에만 존재. 협업 시 불편 |
| 40 | 컴포넌트 Storybook 없음 | `src/app/components/` | 컴포넌트 재사용성 파악 및 디자인 검토 어려움 |
| 41 | README 트러블슈팅 가이드 없음 | `README.md` | 자주 발생하는 문제에 대한 해결 방법 문서 없음 |

### 접근성 (Accessibility)

| # | 항목 | 파일 | 설명 |
|---|------|------|------|
| 42 | 일부 모달 포커스 복원 미구현 가능 | 다수 컴포넌트 | 모달 닫힌 후 포커스가 트리거 요소로 돌아가지 않을 수 있음 |
| 43 | Tailwind 색상 대비율 자동 검사 미설정 | `tailwind.config.ts` | WCAG 기준 대비율 검사 도구 미연동 |

---

## 요약 통계

| 우선순위 | 항목 수 | 주요 내용 |
|--------|--------|---------|
| 🚨 P0 즉시 | 4개 | 하드코딩 시크릿, 취약한 JWT |
| 🔴 P1 높음 | 6개 | 보안 설정, 타입 안전성 |
| 🟠 P2 중간 | 18개 | 성능, 아키텍처, 데이터, 테스트 |
| 🟡 P3 낮음 | 15개 | 코드 품질, DevOps, 문서화 |
| **합계** | **43개** | |

---

## 분석 대상 파일 목록

- `server/index.js` (2031 lines) — 백엔드 전체
- `src/app/App.tsx` — 프론트엔드 진입점
- `src/app/hooks/useAppActions.ts` — 주요 액션 훅
- `src/app/hooks/usePersistentState.ts` — 상태 지속성
- `src/app/lib/projects-api.ts` — API 레이어
- `src/app/lib/project-utils.ts` — 유틸리티
- `src/app/lib/localization.ts` — 날짜/텍스트 현지화
- `src/app/components/ui/chart.tsx` — 차트 컴포넌트
- `src/app/components/OpalProjectDetail.tsx` — 프로젝트 상세
- `src/app/components/ProjectDetailPage.tsx` — 프로젝트 페이지
- `vite.config.ts` — 빌드 설정
- `docker-compose.yml` — 컨테이너 설정
- `package.json` — 의존성
- `.env`, `.env.example` — 환경변수
- `scripts/test-api.mjs` — API 테스트
- 기타 148개+ TypeScript/TSX 파일
